---
import Layout from '../layouts/MainLayout.astro';
---

<Layout title="404 - Strona nie znaleziona">
  <div class="min-h-screen flex items-center justify-center px-6 bg-gradient-to-b from-gray-900 to-black">
    <div class="text-center max-w-2xl">
      {/* Duży numer 404 */}
      <div class="mb-8">
        <h1 class="text-9xl md:text-[12rem] font-bold text-[var(--color-accent)] opacity-20">
          404
        </h1>
      </div>

      {/* Ikona szachownicy */}
      <div class="mb-8 flex justify-center">
        <svg 
          class="w-24 h-24 text-[var(--color-accent)] opacity-50" 
          fill="currentColor" 
          viewBox="0 0 24 24"
        >
          <path d="M4 4h4v4H4V4zm8 0h4v4h-4V4zM4 12h4v4H4v-4zm8 0h4v4h-4v-4zM4 20h4v4H4v-4zm8 0h4v4h-4v-4zm8-16h4v4h-4V4zm0 8h4v4h-4v-4zm0 8h4v4h-4v-4zm-8-8h4v4h-4v-4z"/>
        </svg>
      </div>

      {/* Nagłówek */}
      <h2 class="text-4xl md:text-5xl font-bold text-white mb-4">
        Ups! Strona nie znaleziona
      </h2>

      {/* Opis */}
      <p class="text-xl text-gray-300 mb-8">
        Strona, której szukasz, została przesunięta, usunięta lub nigdy nie istniała. 
        <br class="hidden md:block" />
        Może król zrobił niewłaściwy ruch?
      </p>

      {/* Przycisk powrotu */}
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
        <a 
          href="/"
          class="inline-block bg-[var(--color-accent)] hover:bg-[var(--color-accent)]/80 text-white font-bold px-8 py-4 rounded-lg transition-all duration-300 transform hover:scale-105 shadow-lg"
        >
          ← Wróć na Główną
        </a>
        
        <a 
          href="/contact"
          class="inline-block border-2 border-[var(--color-accent)] text-[var(--color-accent)] hover:bg-[var(--color-accent)] hover:text-white font-bold px-8 py-4 rounded-lg transition-all duration-300"
        >
          Skontaktuj się z nami
        </a>
      </div>

      {/* Dodatkowe linki */}
      <div class="mt-12 pt-8 border-t border-gray-700">
        <p class="text-gray-400 mb-4">Może szukasz:</p>
        <div class="flex flex-wrap justify-center gap-4">
          <a href="/" class="text-[var(--color-accent)] hover:underline">Strona Główna</a>
          <span class="text-gray-600">•</span>
          <a href="/about" class="text-[var(--color-accent)] hover:underline">O nas</a>
          <span class="text-gray-600">•</span>
          <a href="/services" class="text-[var(--color-accent)] hover:underline">Usługi</a>
          <span class="text-gray-600">•</span>
          <a href="/contact" class="text-[var(--color-accent)] hover:underline">Kontakt</a>
        </div>
      </div>
    </div>
  </div>
</Layout>

<style>
  @keyframes float {
    0%, 100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-20px);
    }
  }

  svg {
    animation: float 3s ease-in-out infinite;
  }
</style>